server:
  port: 8761    # Eureka 기본 포트

spring:
  application:
    name: eureka-server

eureka:
  client:
    register-with-eureka: false   # 자기 자신은 등록 안 함
    fetch-registry: false         # 다른 서비스 목록 가져올 필요 없음